// Most used assertions for Cypress testing are:

// - Chai / TDD Assertions: https://docs.cypress.io/guides/references/assertions#Chai
// - Chai-jQuery Assertions: Same link as Above

// Converting Chai assertions into Cypress' .should:

// - Chai: expect($el).to.have.attr('foo', 'bar')
// - Cypress: cy.get('elementX').should('have.attr', 'bar')

// - Chai: expect($el).to.have.class('foo')
// - Cypress: cy.get('elementX').should('have.class', 'class')

